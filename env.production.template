# Production Environment Configuration Template
# 
# INSTRUCTIONS:
# 1. Copy this file to .env: cp env.production.template .env
# 2. Fill in ALL placeholder values with your actual production values
# 3. Generate JWT secret: npm run generate:jwt-secret
# 4. Validate: npm run validate:production
# 5. NEVER commit .env to version control!

# ============================================
# Application Configuration
# ============================================
NODE_ENV=production
PORT=3000
API_URL=https://your-production-api-domain.com

# ============================================
# Database Configuration (PostgreSQL Required)
# ============================================
# Production MUST use PostgreSQL, not SQLite
DB_TYPE=postgres
DB_HOST=your-postgres-host.com
DB_PORT=5432
DB_USERNAME=your-db-username
DB_PASSWORD=your-secure-db-password-min-8-chars
DB_DATABASE=telegram_fitness_db

# Common Database Provider Examples:
# Railway: DB_HOST=containers-us-west-xxx.railway.app
# Heroku: DB_HOST=ec2-xxx.compute-1.amazonaws.com
# DigitalOcean: DB_HOST=db-postgresql-xxx.ondigitalocean.com
# AWS RDS: DB_HOST=your-db.xxx.us-east-1.rds.amazonaws.com

# ============================================
# JWT Authentication
# ============================================
# Generate with: npm run generate:jwt-secret
# MUST be at least 32 characters long
JWT_SECRET=REPLACE_WITH_GENERATED_SECRET_MIN_32_CHARACTERS
JWT_EXPIRES_IN=7d

# ============================================
# OpenAI Configuration
# ============================================
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_PRODUCTION_OPENAI_KEY
OPENAI_MODEL=gpt-4

# ============================================
# Telegram Bot Configuration
# ============================================
# Get from @BotFather on Telegram
# Format: digits:alphanumeric (e.g., 123456789:ABCdefGHIjklMNOpqrsTUVwxyz)
TELEGRAM_BOT_TOKEN=REPLACE_WITH_YOUR_PRODUCTION_BOT_TOKEN

# Optional: Webhook URL (if using webhooks instead of polling)
# TELEGRAM_WEBHOOK_URL=https://your-production-api-domain.com/api/telegram/webhook

# Frontend URL for Telegram Mini App
# This is used for the WebApp button in the welcome message
# Example: https://your-app.vercel.app
TELEGRAM_FRONTEND_URL=https://your-frontend-url.com

# ============================================
# CORS Configuration
# ============================================
# List of allowed origins (comma-separated, no spaces)
# MUST use HTTPS in production
# Include your frontend URL and Telegram Mini App URL
CORS_ORIGIN=https://your-frontend-domain.com,https://your-telegram-miniapp-url.com

# ============================================
# Security Checklist
# ============================================
# Before deploying, verify:
# [ ] All REPLACE_WITH_* values are filled in
# [ ] JWT_SECRET is 32+ characters (use npm run generate:jwt-secret)
# [ ] DB_PASSWORD is 8+ characters
# [ ] API_URL uses HTTPS
# [ ] CORS_ORIGIN uses HTTPS URLs
# [ ] No localhost URLs in production config
# [ ] All secrets are different from development

